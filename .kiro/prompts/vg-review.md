Get the latest ViewGraph capture and check for annotations using get_annotations. For each annotation, use get_annotation_context to get the full DOM context around the annotated elements. Then implement the fixes described in the annotation comments. Work through them one at a time, showing what you're changing and why.
